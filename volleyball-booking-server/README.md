# Volleyball Booking Server (Stripe PaymentIntent)

## Setup
1. Copy `.env.example` to `.env` and set `STRIPE_SECRET_KEY`.
2. Install and run:

```bash
npm install
npm run dev
```

The server runs on `http://localhost:4242` by default. Point the app to it via `EXPO_PUBLIC_API_BASE_URL`.
